For project setup 

python -m venv lg-env                                      #creating a virtual environment
source lg-env/bin/activate or lg-env/Scripts/activate      #activating the virtual environment
pip install -r requirements.txt                            #installing the required packages
